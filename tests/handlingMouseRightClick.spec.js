import {test, expect} from '@playwright/test'

test ('Right Click', async({page}) => {

    // const button = page.locator("locator address")
    // await button.click({button: 'right'})
    // await page.waitForTimeout()
   



    await page.waitForTimeout(5000);
})