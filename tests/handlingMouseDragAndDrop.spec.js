import {test, expect} from '@playwright/test'

test("Drag and Drop", async({page}) => {

    // await page.goto("URL");
    // const sourceElement = page.locator("Locate the element to drop here");
    // const targetElement = page.locator("locate the place to drop here");

    // await sourceElement.dragAndDrop(targetElement);

    // const droppedElement = targetElement.locator("locate the element after dropping");
    // await expect(droppedElement).toBeVisible


    
})